import React, {FC} from "react";
import {OptionTemplate} from "../templates";

// Use this to communicate with Background and to manage state for asynchronous operations.
export const OptionPage: FC = () => <OptionTemplate/>;
